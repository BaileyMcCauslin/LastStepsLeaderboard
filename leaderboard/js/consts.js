// Import the necessary environment variables
import * as ENV from "../env.js";

// Export environment variables
export const ALL_USR = ENV.ALL_RECORDS;
export const GET_USR = ENV.GET_USER_RECORD;
export const REG_USR = ENV.REG_USER;
